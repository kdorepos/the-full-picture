import { defineConfig } from 'astro/config';

// Static output (default) — deploys to Vercel with zero config via its Astro preset.
export default defineConfig({
  site: 'https://the-full-picture.vercel.app',
});
